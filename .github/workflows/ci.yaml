name: CI Pipeline

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main

permissions:
  contents: write

jobs:
  sast:
    if: github.event_name == 'pull_request'
    uses: ElderPing/elderping-shared/.github/workflows/_sast.yml@v1.0.0
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_URL: ${{ secrets.SONAR_URL }}

  sca:
    if: github.event_name == 'pull_request'
    needs: sast
    uses: ElderPing/elderping-shared/.github/workflows/_sca.yml@v1.0.0
    with:
      service-name: elderpinq-reminder-service
      service-path: .
      runtime: node
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  notify-snyk:
    if: github.event_name == 'pull_request' && needs.sca.outputs.critical-found == 'true'
    needs: sca
    uses: ElderPing/elderping-shared/.github/workflows/_notify.yml@v1.0.0
    with:
      subject: "Snyk Critical Vulnerabilities Found - elderpinq-reminder-service"
      body: "Critical or high vulnerabilities were found in elderpinq-reminder-service. Please review the attached report."
      service-name: elderpinq-reminder-service
      artifact-name: snyk-report-elderpinq-reminder-service
    secrets:
      BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}

  build:
    if: github.event_name == 'pull_request'
    needs: [sast, sca]
    uses: ElderPing/elderping-shared/.github/workflows/_docker-build.yml@v1.0.0
    with:
      service-name: elderpinq-reminder-service
      service-path: .

  notify-trivy:
    if: github.event_name == 'pull_request' && needs.build.outputs.trivy-critical == 'true'
    needs: build
    uses: ElderPing/elderping-shared/.github/workflows/_notify.yml@v1.0.0
    with:
      subject: "Trivy Critical Vulnerabilities Found - elderpinq-reminder-service"
      body: "Critical or high vulnerabilities were found in the Docker image for elderpinq-reminder-service. Please review the attached report."
      service-name: elderpinq-reminder-service
      artifact-name: trivy-elderpinq-reminder-service
    secrets:
      BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}

  publish:
    if: github.event_name == 'push'
    uses: ElderPing/elderping-shared/.github/workflows/_docker-publish.yml@v1.0.0
    with:
      service-name: elderpinq-reminder-service
      service-path: .
      environment: ${{ github.ref_name }}
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
